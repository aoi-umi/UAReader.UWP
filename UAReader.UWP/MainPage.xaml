<Page
    x:Class="UAReader.UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UAReader.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <SplitView Name="mainSplitView" CompactPaneLength="0" OpenPaneLength="100" DisplayMode="CompactOverlay">
            <SplitView.Pane>
                <ListView Name="mainNavigationList" SelectionMode="Single" IsItemClickEnabled="True" ItemClick="mainNavigationList_ItemClick">
                    <ListViewItem Tag="menu" Name="menuItem" Background="AliceBlue">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE700;"
                                       Style="{StaticResource MenuIcon}"/>
                        </StackPanel>
                    </ListViewItem>
                    <ListBoxItem>
                        aaa
                    </ListBoxItem>
                </ListView>
            </SplitView.Pane>
            <SplitView.Content>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>
                    <RelativePanel Height="44" Background="{ThemeResource SystemControlHighlightAltListAccentLowBrush}">
                        <TextBlock Text="&#xE700;" Padding="12 16" Style="{StaticResource MenuIcon}" Tapped="Menu_Tapped"/>
                    </RelativePanel>
                </Grid>
            </SplitView.Content>
        </SplitView>
        <!--<TextBlock Margin="12 16 0 0" Text="&#xE700;" VerticalAlignment="Top"
                                       Style="{StaticResource MenuIcon}"/>-->
    </Grid>
</Page>
